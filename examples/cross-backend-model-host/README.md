# cross-backend-model-host

Generated models with schema `database:` evaluators spanning multiple backends.

## Audience

Integrators routing tables across heterogeneous engines.

## Run

```bash
cargo check -p cross-backend-model-host
```

See `DatabaseBackend` / `DatabaseRouter` rustdoc (`cargo doc -p uf-valence-core --open`).
